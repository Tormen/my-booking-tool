# Local-only files: never published, carried between machines by
# the container repo (see its .git.force-add-patterns). Everything
# personal in this checkout is named this way -- see README.md and
# app/local_overlay.py.
*.local
*.local.*
